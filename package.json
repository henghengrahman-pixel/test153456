{
  "name": "livechat-ai-railway",
  "version": "1.29.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-all.mjs",
    "verify": "npm run check && npm test -- --runInBand && npm run audit",
    "audit": "node scripts/release-audit.mjs"
  },
  "dependencies": {
    "express": "5.1.0",
    "pg": "8.16.3"
  }
}
