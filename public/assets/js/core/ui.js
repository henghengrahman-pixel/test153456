export function toast(message,type='info'){let h=document.querySelector('.toast-host');if(!h){h=document.createElement('div');h.className='toast-host';document.body.append(h)}const x=document.createElement('div');x.className=`toast ${type}`;x.textContent=message;h.append(x);setTimeout(()=>x.remove(),3200)}
export function esc(v=''){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
export function confirmTyped(title,phrase){return new Promise(resolve=>{const d=document.createElement('dialog');d.innerHTML=`<h3>${esc(title)}</h3><p class="muted">Ketik <b>${esc(phrase)}</b> untuk melanjutkan.</p><input autocomplete="off"><div class="toolbar" style="margin-top:14px"><button class="btn danger" disabled>Konfirmasi</button><button class="btn ghost">Batal</button></div>`;document.body.append(d);const input=d.querySelector('input'),yes=d.querySelector('.danger'),no=d.querySelector('.ghost');input.oninput=()=>yes.disabled=input.value!==phrase;yes.onclick=()=>{d.close();d.remove();resolve(true)};no.onclick=()=>{d.close();d.remove();resolve(false)};d.showModal();input.focus()})}
